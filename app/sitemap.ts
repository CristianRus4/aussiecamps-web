import type { MetadataRoute } from "next";import { SITE_URL,articles } from "@/lib/site";
export default function sitemap():MetadataRoute.Sitemap{const now=new Date("2026-08-12");return["","/guides","/journal","/support","/privacy","/terms"].map(path=>({url:`${SITE_URL}${path}`,lastModified:now,changeFrequency:path===""?"weekly":"monthly",priority:path===""?1:.7})).concat(articles.map(a=>({url:`${SITE_URL}/journal/${a.slug}`,lastModified:now,changeFrequency:"monthly" as const,priority:.75})));}
